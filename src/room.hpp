class Room{
    private:
        char name,
        char status,
        char type;
        char description;
        vector<Room*> border;
        vector<Container> container;
        vector<Item> item;
        vector<Creature> creature;
        trigger[]
}
